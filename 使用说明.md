# 时尚百货城登录自动化

当前发布版本：**1.1.0**（2026-08-26）。

该项目基于 MaaFramework，通过 MuMu 模拟器的 ADB 接口执行以下流程：

1. 在模拟器桌面识别并点击“时尚百货城”。
2. 等待账号登录页。
3. 在控制台读取账号和隐藏输入的密码，依次填入游戏。
4. 勾选“我已详细阅读并同意”，点击登录。
5. 按客户端填写的目标区号打开选服列表；通过 OCR 上下滑动查找并选择对应区服，然后点击开始。
6. 如果出现底部带“今日不再提示”或“本次登录不再提示”的提示弹窗，立即勾选对应选项；前者点击右上角关闭，后者点击全屏赠礼的安全空白区域关闭。
7. 处理离线收益弹窗。
8. 如果出现每月签到，通过“本月累计”“本月可补”等稳定文字确认页面，再点击固定的签到按钮中心并处理随后出现的“确定”；首次点击未生效时最多重试一次，已签到时关闭签到页。
9. 如果出现幸运赠礼，持续点击屏幕中心跳过。
10. 通过底部“百货、商业街、关卡、伙伴、影视城、背包”等 Tab 组合确认进入主界面。
11. 主界面识别通过后，直接点击右侧日常剪贴板图标中心 `(640,970)`；不再依赖可能被角标或标注遮挡的“日常”文字 OCR。
12. 进入日常页后，只检索并点击“领取”按钮，处理可能出现的“确定”，随后向下滚动任务列表继续领取；连续两次滑动画面不再变化时判定到底。

### 日常计划

程序会在进入日常后按底部 Tab 分组执行，不在任务执行前搜索“已完成”状态。它先 OCR 定位计划任务标题，再从当前视口所有“前往”中选择与标题纵向中心最近的同行文本框，点击该文本框正中心并等待 2 秒；本轮无法取得同行文本框时才回退到原相对偏移点击，避免把任务标题与其他行按钮错误组合。点击后必须识别到该任务对应的目标页面才算跳转成功，不能再以“日常页暂时未识别到”代替目标确认。若明确仍停留日常页，则判定本次点击未生效并重复点击，初次点击加四次重试共五次后才失败；既未识别到日常页也未识别到目标页时会保守停止。百货组在一次进入中依次处理生鲜超市升级/进货、幸运扭蛋和私人会馆兑换；进入百货后会先检查当前视口，只要识别到“生鲜”即可命中。未找到时先复位到楼层左上角，再以左右、向下交替的蛇形路径扫描二维场景。点击生鲜店铺后会预留约 8.5 秒让界面稳定，并在最长 30 秒的确认窗口中组合识别“基础收入/其他加成/生鲜”和“升级/进货/业务/员工”，避免加载较慢或艺术字 OCR 不稳定时误判。页面确认成功后，“进货”使用右上角固定按钮位置点击，并继续验证进货界面是否真正打开；进货界面返回生鲜超市以及生鲜超市返回百货，均点击左下角返回按钮中心附近的固定坐标 `(50,1230)`，随后确认目标页面，未跳转时按统一跳转逻辑重试。影视城组处理艺人宣传；伙伴组从三列固定位置有限尝试伙伴，进入详情后先区分“升级”和“晋升”，只有识别到“升级”才继续，只有“晋升”时返回列表切换其他伙伴；关卡组处理工厂研发。每组优先 OCR 识别并点击底部“商业街”文字框中心回到城市主页，OCR 未命中时才使用固定坐标 `(200,1215)` 兜底；随后统一进入日常核验并领取奖励。日常任务入口扫描连续两次滑动画面不变时会判定到底并停止，不再重复空滑；百货店铺查找也有固定扫描上限，不会无限滑动。

当前实现的任务：`任意店铺升级10次`、`生鲜超市进货1次`、`幸运扭蛋抽奖1次`、`私人会馆商店兑换1次商品`、`艺人宣传3次`、`名媛会培育1次`、`关卡工厂研发5次`，以及登录自然完成。伙伴升级的原实现保留但当前停用；商战击败伙伴、环球差旅和伙伴培训暂不执行。名媛会培育完成后会处理庄园补给：进入时已购买则直接收尾，否则讲价后仅在红框价格明确为负数时购买。

每项消费动作最多执行一次，并要求先确认“连升十级”“一键进货”“一键宣传”等选项的绿色勾；奖励层统一点击左上安全空白处关闭，避开“再次进货”“去上架”“抽十次”等按钮。最终领取可领取任务奖励后，只有识别到日常钢笔下方活跃度为 100、成功领取最右侧 100 礼包并关闭奖励层，才会尝试停止当前前台游戏应用；活跃度不足或前台包名无法确认时不会退出游戏。多账号模式下，只有当前账号完全完成且游戏安全关闭后，程序才会重新打开游戏并登录列表中的下一个启用账号；任何一步未满足都会停止账号队列。

1. `关卡工厂研发5次`：在对应任务行点击“前往”，确认进入关卡工厂后点击“研发”5次；随后通过底部“百货”返回主页面，再进入日常领取奖励。

点击开始后会立即轮询离线收益；连续 20 秒仍未识别到时，按本次不出现处理并继续。若底部带“今日不再提示”或“本次登录不再提示”的弹窗、签到或幸运赠礼先出现，会先处理，同时继续等待离线收益直至达到该上限。“今日不再提示”弹窗会先勾选底部选项，再关闭右上角；“本次登录不再提示”全屏赠礼会先勾选底部选项，等待 1 秒让状态稳定后再点击左上安全空白区域关闭。关闭后会同时检查“恭喜获得”和原赠礼页：若原页仍在，只重试关闭位置而不再触碰勾选框，最多三次；若原页已消失，则只等待奖励层，避免误触底层页面。“恭喜获得”属于可选后续弹窗：识别到时才点击，并等待界面稳定、最多重试关闭三次；预留等待窗口后仍未出现时只写日志并继续，不再视为失败。登录赠礼可能在奖励层关闭后立即再次出现，因此程序会把每次出现都作为新一轮完整处理，循环勾选、关闭并领取，直到当前界面不再被遮挡；不会再把新一轮赠礼误判成上一轮关闭失败。该全屏赠礼在日常流程运行途中仍可能延迟出现，因此通用任务、识别和可选动作失败时会先检查它；确认被遮挡后会完整清理当前连续出现的赠礼、写入恢复日志并原地重试原动作，同一动作后续再次遇到赠礼时仍会重复清理。单批赠礼连续超过 30 轮，或同一动作累计恢复超过 30 次时会保守停止并写入日志，避免错误识别造成无限点击。离线收益已处理或等待超时后，程序仍会继续清理弹窗；弹窗一旦出现便立即点击，并从该次点击后重新计算静默时间。若离线收益在这段清理窗口内迟到，也会正常处理；只有连续 2 秒没有识别到任何弹窗，才验证主界面并判定流程完成。

## 安全说明

- 按用户要求，客户端会把账号队列、密码、目标区号和启用状态以明文写入本机 `runtime/config/client_config.json`，不会由程序上传网络。
- 客户端提供“清除本地配置”按钮，可删除该文件并清空界面。
- 密码通过 `getpass` 隐藏输入。
- 凭据通过无交互 ADB Shell 的标准输入发送，命令行参数和控制台均不包含密码。
- 为避免 MaaFramework 输入动作记录敏感文本，运行器会强制关闭文件日志和失败截图。
- 项目不会保存用户提供的登录截图。
- 请确保自动化用途符合游戏服务条款。

## 当前限制

- 当前版本按照 MaaFramework 的 720×1280 竖屏识别坐标编排，使用 MuMuManager 提供的模拟器专用截图通道；游戏打开并到达登录页后才会检查分辨率，打开游戏前的模拟器桌面分辨率不参与校验。1080×1920、1440×2560、2160×3840 等 9:16 竖屏游戏分辨率会自动缩放到该识别画布，点击坐标由 MaaFramework 映射回原始分辨率。普通 `emulator-5554` ADB 截图在部分 MuMu 版本上不可用。
- 自动化会在正式动作前及识别失败恢复时检查中断弹窗；检测到画面底部“点击任意处关闭”后，会点击画布中心并确认提示文字消失，再继续原流程。
- 某项游戏内任务识别失败、且未命中任何已知弹窗时，会在左下返回键固定位置 `(50,1230)` 执行最多三轮点击；每轮后立即重新判断原任务，恢复成功便停止点击，三轮后仍失败才终止任务。“打开游戏到登录页”不启用该兜底，避免在模拟器桌面误开其他应用。
- 用户协议、连升十级、一键进货、一键宣传、伙伴连升十级等勾选操作都会记录检查目标、初始状态、点击动作、逐次复核和最终状态；检查前后也会保存调试截图。只有明确确认选中后，才会继续登录、升级、进货或宣传等后续动作。
- 主界面分别检查左下“百货”、底部“关卡/伙伴”和底部“影视城/背包”三个导航 OCR 锚点，并额外检查右侧固定入口“日常/商店”；只有导航至少两项命中且固定入口命中，才确认到达主页。这样既容忍单个艺术字偶发漏识别，也避免伙伴、艺人等同样带底部导航的子页面被误判成主页。
- 首次打开日常页后，运行器会先回到列表顶部，再逐屏滚动到底部盘点任意店铺升级、生鲜超市进货、幸运扭蛋、私人会馆兑换、艺人宣传、伙伴升级和关卡工厂研发。任务标题与右侧“前往／领取／已领取”会按同行坐标关联；只有“前往”进入执行计划，“领取”和“已领取”跳过具体任务并在收尾阶段统一处理奖励，未识别状态会保守跳过并写入警告日志。
- 完成日常并领取 100 活跃礼包后，运行器会读取前台窗口包名，仅停止已确认的当前游戏应用，不会关闭模拟器，也不会关闭自动化客户端；若账号队列中还有启用账号，客户端会重新打开游戏继续下一项，否则回到“已完成”状态。若游戏包名需要额外约束，可运行前设置 `$env:FASHION_MALL_PACKAGE`；包名不匹配时程序会保守保留游戏运行并停止后续账号。
- 登录页的账号框、密码框已用临时测试文本完成实机输入与识别验证，测试内容随后已清空。
- 选服及弹窗节点已通过用户截图的离线 OCR 验证，仍需执行一次完整实机登录来校准点击后的页面切换。
- 区服列表初始位于最大区号所在的顶部。当前画面未命中时，会在列表中央区域用 `(360,850) → (360,450)` 的手势沿列表内容向下查找更小区号，避免触碰底部分类栏；走到底仍未找到时再在同一区域反向回扫。选中后必须同时识别到目标区号和“点击开始”，确认已经返回启动页，才会进入游戏。
- 首次实机测试后，可能需要根据 OCR 日志微调 ROI 或文字替换规则。

## 环境准备

便携版用户只需完整解压并双击 `FashionMallClient.exe`，无需安装 Python。开发者从源码运行时，在本目录创建虚拟环境并安装 Python 包：

```powershell
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
```

不要从源码构建 MaaFramework。

从官方 MaaCommonAssets 获取 `ppocr_v5/zh_cn` 中文 OCR 模型，将以下文件放入 `resource/model/ocr/`：

```text
det.onnx
rec.onnx
keys.txt
```

## 运行

### 桌面客户端（推荐）

双击项目目录中的 `启动客户端.bat`。首次在窗口内输入账号、密码和目标区号；点击“＋ 添加账号”可以延长账号队列，每行右侧的“使用”勾选决定该账号本轮是否激活。点击“开始运行”后，客户端会保存完整账号队列并按顺序执行所有启用账号；以后启动会自动读取，不需要重复输入。“删除”可移除不再需要的账号，“停止”会在当前识别动作结束后安全中止整个队列。

客户端运行时会在 `runtime/debug/screenshots-会话时间/` 中启用逐步调试截图：Maa 任务节点成功或失败、固定点击或滑动以及 ADB 输入完成后，会立刻保存按顺序编号的 PNG，并在会话日志中写入截图编号与步骤名称。截图仅用于本次运行排障；正常关闭客户端时会删除 `runtime/debug/` 下全部 `screenshots-*` 调试截图目录。程序异常退出时截图会保留，便于定位最后停留画面。

客户端每次启动都会在 `runtime/debug/` 创建带时间戳的会话诊断日志，记录流程节点、日常扫描目标、扫描次数和识别结果，不记录账号或密码。账号可通过每行左侧“↕ 序号”拖拽调整顺序；点击开始后拖拽锁定，任务按当时的列表顺序执行。正常关闭客户端时会等待任务线程结束并删除本次日志；若程序崩溃或被强制结束，日志会保留以便事后排查。日志文件的完整路径会显示在“运行状态”顶部。

本地配置是用户明确授权的明文凭据文件；MaaFramework 文件日志仍保持关闭。当前客户端直接使用项目自带的 `.venv`，无需打开 PowerShell。

### 命令行

1. 启动 MuMu 模拟器并保持在桌面或登录页。
2. 在本项目目录执行：

```powershell
.\.venv\Scripts\python.exe .\runner.py
```

3. 首次运行按提示输入账号、密码和目标区号；之后命令行会自动读取本地账号队列，并按顺序执行其中启用的账号。

输入账号密码前请将 Windows 输入法切换为英文。当前安全输入方式支持可打印 ASCII 字符；账号或密码为空时，程序会在连接模拟器前停止。

运行器会优先调用 MuMuManager 并自动选择 `MuMu安卓设备-MuMuPlayer v5+`，不再要求手工选择 `emulator-5554`。如果使用的不是当前 MuMu 安装，可通过 `ADB_PATH` 指定普通 ADB 作为兼容回退：

```powershell
$env:ADB_PATH = "新的 adb.exe 绝对路径"
```

为防止凭据进入日志，文件日志和失败截图默认关闭；控制台只显示流程节点和不含凭据的错误信息。

## 需要补充的素材

- 通过 ADB 获取的登录页、选服页、离线收益、幸运赠礼和主界面原始截图。
