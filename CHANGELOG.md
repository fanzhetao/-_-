# 更新记录

## 未发布

- 将每个账号的花房培育配置改为四档复选；支持任意选择一个或多个档位，新账号默认执行入门、初级和中级培育，旧版单档配置会自动兼容。
- 花房培育流程改为进入一次后按固定顺序连续完成全部已选档位，再统一返回名媛会。
- 优化账号队列拖动排序：加入防误触阈值、越过相邻行中心后再换位、拖动高亮和列表边缘自动滚动。
- 名媛会入口支持识别底部“未加入名媛会”提示，关闭弹层后跳过培育与庄园补给并继续后续日常流程。
- 新增登录后商战前置流程：主页文字识别与固定坐标兜底进入，确认快速战斗选项，执行竞争并逐层关闭结果弹层，最后安全返回主页。
- 将商战连续结果弹层的关闭点击位置从阵容中央下移至底部提示区域，确保“点击任意处关闭”能够稳定触发。

## 1.1.1 — 2026-08-27

- 新增可独立开关的错误诊断包：立即中断和错误恢复两种模式均可将最近 5 步操作与截图保存为“账号名_时间.zip”。
- 将运行器、配置、设备发现、Maa 作业、日常规则、校验、重试和客户端状态职责拆分到 `fashion_mall/` 包，保留 `client.py` 与 `runner.py` 根入口兼容现有启动方式和调用。
- 优化客户端 UI、账号队列、线程取消传播，以及自动化流程的通用识别、执行、确认和恢复重试骨架。
- 新增轻量便携版自检路径，仅校验发布目录中的版本、OCR 资源和 Maa 运行库，不初始化界面、设备或账号。
- 修复 Windows 路径包含空格时便携版自检 marker 参数传递不完整的问题。
- 完成 123 项单元测试、PyInstaller 便携版自检、敏感文件审计、ZIP 内容审计和 SHA-256 复核门禁；MuMu 与真实游戏端到端验证仍待实机执行。

## 1.1.0 — 2026-08-26

- 账号列表支持拖拽排序，并在任务运行期间锁定顺序。
- 登录阶段可识别“账号不存在”和“密码错误”，登录后的无弹窗确认时间缩短为 2 秒。
- 每月签到支持自动领取已达到的累计签到奖励。
- 停用伙伴升级任务但保留原实现，新增可配置档位的名媛会花房培育流程。
- 新增庄园补给讲价和安全购买流程；仅在价格明确为负数时购买，已购买时直接完成收尾。
- 名媛会和花房培育装饰字入口增加固定坐标路径，避免引导手指和 OCR 波动造成卡点。
- 店铺达到等级上限时自动完成一次拓展，再执行连升十级，并返回日常复核任务是否真正完成。
- 将配置读写、旧格式迁移和无界面 UI 辅助逻辑抽为 `fashion_mall/` 包，保留根入口兼容启动与既有调用。
- 新增 `docs/架构说明.md`，明确源码、资源、测试、运行数据和构建产物的边界。

## 1.0.0 — 2026-08-25

- 首个正式便携版。
- 提供 Tkinter 桌面客户端及多账号顺序执行。
- 支持登录、选服、常见弹窗处理和已实现的日常任务流程。
- 支持 MuMu 模拟器 12 与 720×1280、1080×1920、1440×2560、2160×3840 的 9:16 竖屏游戏分辨率。
- 发布包包含 Python、MaaFramework、中文 OCR 模型和运行所需原生库。
- 提供一键发布脚本，自动执行测试、构建、自检、发布包审计和 SHA-256 复核。
- 远程源码仓库不再跟踪测试目录、本机运行配置或构建产物。
- 本地账号和密码仍以明文保存在程序目录，请仅在可信设备上使用。
